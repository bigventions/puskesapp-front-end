// Import Dependencies

export const Label = ({
  // Props
  children,
  id,
}) => {
  // State

  // Hooks

  // Methods

  // Event Handler

  // Return JSX
  return (
    <label
      htmlFor={id}
      className="block mb-2 text-sm font-medium text-text"
    >
      {children}
    </label>
  );
};
